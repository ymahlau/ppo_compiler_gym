name: ppo_compiler_env
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.9
  - pip
  - swig
  - git
  - pip:
    - setuptools==65.5.0
    - torch
    - matplotlib
    - gym==0.21.0
    - numpy
    - compiler_gym==0.2.1
    - tqdm